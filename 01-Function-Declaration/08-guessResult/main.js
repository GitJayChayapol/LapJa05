function squareNum(num) {
  const result = num * num;
}

console.log(squareNum(2)); // ผลลัพธ์ เป็น Undefined ไม่สามารถแสดงผลลัพธ์ได้ เพราะ result ไม่ทำงาน แต่หากเปลี่ยน const เป็น return reslut จะสามารถทำงานได้

//return result = num * num;
